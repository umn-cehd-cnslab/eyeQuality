% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tobii-pro-adapter.R
\name{.tobii_pro_confidence}
\alias{.tobii_pro_confidence}
\title{Compute the generic \code{confidence} (0-1) columns from Tobii Pro's native
\code{"Valid"}/\code{"Invalid"} validity strings}
\usage{
.tobii_pro_confidence(data)
}
\arguments{
\item{data}{standardized data.frame with \code{validityLeft}/\code{validityRight}.}
}
\value{
\code{data} with new \code{confidenceLeft}/\code{confidenceRight} columns.
}
\description{
\code{confidenceLeft}/\code{confidenceRight <- ifelse(validityLeft/validityRight == "Valid", 1, 0)} -- see the \code{confidence} entry in \code{?eyeQuality-schema} for
the full 0-1 design rationale. As on the Tobii Studio adapter (see
\code{.tobii_studio_confidence()}), this emits per-eye \code{confidenceLeft}/
\code{confidenceRight} columns rather than a single combined \code{confidence}
value, consistent with every other per-eye pair in the schema.

\strong{NA-validity-to-confidence mapping (P3-06 decision):} \code{confidence} is
set to \code{1} for \code{NA} validity, matching option (a) in the OPEN DECISION
section of \code{?eyeQuality-schema}. Rationale: the \emph{existing} \code{.valid}-
masking behavior (\code{.tobii_pro_mask_eye()} above, ported unchanged from
\code{removeInvalidGaze()}) already treats \code{NA} validity as unmasked --
effectively "valid" -- because \code{validityCol == "Invalid"} is \code{NA} when
\code{validityCol} is \code{NA}, and \code{tidyr::replace_na(replaceRows, FALSE)} then
coerces that \code{NA} to \code{FALSE}, so the sample is not masked.
\code{confidence = 1} for \code{NA} validity keeps this new column in agreement
with what masking actually does today, rather than introducing a second,
disagreeing notion of validity for the same input. See
\code{.tobii_studio_confidence()}'s roxygen for the full reasoning behind
choosing this option over \code{confidence = 0} -- identical logic applies
here; this is the same deliberate, documented choice made consistently
for both adapters, not an independent per-adapter decision.
}
\keyword{internal}
