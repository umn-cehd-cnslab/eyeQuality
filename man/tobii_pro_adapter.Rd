% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tobii-pro-adapter.R
\name{tobii_pro_adapter}
\alias{tobii_pro_adapter}
\title{Tobii Pro eye tracker adapter}
\description{
\code{detect()} is ported unchanged from the pre-Phase-3 \code{detectImportSourceType()}
\code{if}/\verb{else} branch: raw (pre-standardization) Tobii Pro exports carry a
\code{"Recording software version"} column that no other supported device
produces.

\code{standardize()} is ported unchanged (P3-04) from the pre-Phase-3
\code{renameColumns()}'s \code{TobiiPro} branch (\code{R/renameColumns.R}): renames the
device-native columns onto the generic schema (\code{?eyeQuality-schema}).

\code{extract_events()} is ported unchanged (P3-05) from the pre-Phase-3
\code{extractEventRows()}'s \code{TobiiPro} branch (\code{R/extractEventRows.R}): splits
standardized data into gaze-stream vs. event rows using the \code{Sensor}
column. Unlike the old function, this adapter method takes no \code{software}
argument to dispatch on -- the registry (\code{detectImportSourceType()},
P3-03) already selects this adapter specifically for Tobii Pro data, so
there is no "unrecognized software value" branch to carry forward here;
see the roxygen note on \code{.tobii_pro_extract_events()} below for the full
reasoning.

\code{normalize_validity()} is ported unchanged (P3-06) from the pre-Phase-3
\code{removeInvalidGaze()}'s \code{TobiiPro} branch (\code{R/removeInvalidGaze.R}): for
each eye, samples with a \code{"Invalid"} \code{validityLeft}/\code{validityRight} value
are masked to \code{NA} in new \code{.valid}-suffixed columns (P1-05's decision;
raw columns are left untouched). It additionally computes the new
\code{confidenceLeft}/\code{confidenceRight} (0-1) columns from
\code{?eyeQuality-schema} -- see \code{.tobii_pro_confidence()} below for the
NA-validity mapping decision this task resolves.
}
\keyword{internal}
