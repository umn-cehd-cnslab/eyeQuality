% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/batchConfig.R
\name{write_batch_config}
\alias{write_batch_config}
\title{Write a \code{batch_config.yaml} file}
\usage{
write_batch_config(config, path)
}
\arguments{
\item{config}{a named list; see \code{?read_batch_config} for the full field
list. Any field omitted is filled in from \code{default_batch_config()}
before writing -- explicit \code{NULL} values in \code{config} are preserved
(not overwritten by a non-\code{NULL} default).}

\item{path}{character(1) file path to write to (parent directory created
if it doesn't already exist).}
}
\value{
\code{path}, invisibly.
}
\description{
Fills in any field \code{config} omits with \code{default_batch_config()}'s
defaults, validates the result (\code{validate_batch_config()}), and writes it
to \code{path} as YAML. Writing an invalid config (e.g. missing \code{batchName})
is refused with the same clear, complete error \code{validate_batch_config()}
would raise, rather than writing a file that would only fail later on
load.
}
