% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/adapter-interface.R
\name{.diagnose_all_na_columns}
\alias{.diagnose_all_na_columns}
\title{Diagnose schema columns present but entirely NA (P3-10)}
\usage{
.diagnose_all_na_columns(data, cols, verbose)
}
\arguments{
\item{data}{data.frame to check.}

\item{cols}{character vector of column names to check (only those
actually present in \code{data} are checked; this deliberately does not
flag columns that are missing entirely, since a required-but-absent
column is a separate, already-loud failure mode elsewhere in the
pipeline, not a silent-data-quality one).}

\item{verbose}{logical(1). When not \code{TRUE}, this function is a no-op.}
}
\value{
\code{invisible(NULL)}.
}
\description{
For each column name in \code{cols} that exists in \code{data}, reports (in
verbose mode) when every value in that column is \code{NA} -- the "column
\code{PupilLeft} present but 100\% NA" case from the plan: the column made it
through import (so it isn't simply missing/misnamed), but carries no
usable data, which a user skimming only the final QC summary would not
otherwise be pointed at directly.
}
\keyword{internal}
