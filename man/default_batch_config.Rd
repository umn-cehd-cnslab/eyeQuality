% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/batchConfig.R
\name{default_batch_config}
\alias{default_batch_config}
\title{Default \code{batch_config.yaml} contents}
\usage{
default_batch_config()
}
\value{
a named list; see \code{?read_batch_config} for the full field list
}
\description{
Returns a config with \code{schemaVersion} set and every optional field at the
same default \code{listBidsFiles()}/\code{eyeQuality()}/\code{eyeQualityBatch()} would
use if that field were left unspecified. Fields with no sensible
package-wide default (\code{batchName}, \code{directoryBIDS},
\code{displayDimensionX_mm}, \code{displayDimensionY_mm}) are \code{NULL} -- a fresh
config built from this is a starting point to fill in, not already a
valid one (\code{validate_batch_config()} will say so). Used internally by
\code{read_batch_config()}/\code{write_batch_config()} to fill in any field a
caller/file omits, and exported as a convenience for anything (e.g. a
future Shiny form) that wants to pre-populate a blank config.
}
