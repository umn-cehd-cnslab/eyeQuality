% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tobii-studio-adapter.R
\name{.tobii_studio_mask_eye}
\alias{.tobii_studio_mask_eye}
\title{Mask invalid/out-of-threshold samples for one eye (Tobii Studio)}
\usage{
.tobii_studio_mask_eye(data, whichEye, threshold)
}
\arguments{
\item{data}{standardized data.frame (post \code{standardize()}).}

\item{whichEye}{\code{"left"} or \code{"right"}.}

\item{threshold}{numeric, on Tobii Studio's native \code{0}-\code{4} validity scale.}
}
\value{
\code{data} with new \code{.valid}-suffixed columns for \code{whichEye}.
}
\description{
Ported unchanged from the pre-Phase-3 \code{removeInvalidGaze()}'s
\code{TobiiStudio} branch (\code{R/removeInvalidGaze.R}): every raw column whose
name contains \code{whichEye} (case-insensitive) and is not itself a validity
column gets a \code{.valid}-suffixed copy, masked to \code{NA} wherever the
matching eye's \code{validityLeft}/\code{validityRight} value exceeds \code{threshold},
plus the \code{-9999} missing-value sentinel masked to \code{NA}. Raw columns are
left byte-identical (P1-05).
}
\keyword{internal}
