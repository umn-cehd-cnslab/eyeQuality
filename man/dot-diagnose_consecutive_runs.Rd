% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/adapter-interface.R
\name{.diagnose_consecutive_runs}
\alias{.diagnose_consecutive_runs}
\title{Diagnose contiguous runs of flagged samples (P3-10)}
\usage{
.diagnose_consecutive_runs(flag, label, verbose, min_run_length = 5)
}
\arguments{
\item{flag}{logical vector, same length as the data being diagnosed;
\code{NA} is treated as not-flagged (consistent with this package's existing
\code{NA}-passes-through validity-masking behavior, see \code{?eyeQuality-schema}).}

\item{label}{character(1) description of what \code{flag == TRUE} means,
inserted into the emitted message (e.g. \code{"left eye validity below threshold (2)"}).}

\item{verbose}{logical(1). When not \code{TRUE}, this function is a no-op.}

\item{min_run_length}{integer(1). Minimum contiguous run length to report
individually. Default \code{5}.}
}
\value{
\code{invisible(NULL)}.
}
\description{
Given a logical vector where \code{TRUE} marks a sample flagged for some
data-quality reason (below a validity threshold, offscreen, missing in
both eyes, etc.), reports both a total count and, for any contiguous run
at least \code{min_run_length} samples long, a "rows \code{start}-\code{end}: \code{n}
consecutive samples ..." line -- e.g. the plan's illustrative "rows
1402-1389: N consecutive samples with validity below threshold" (row
order here is always ascending start-end, not the plan's example order).
Short, scattered single/few-sample flags are summarized only in the total
count, not individually, to keep verbose output genuinely diagnostic
rather than a line-per-row flood.
}
\keyword{internal}
