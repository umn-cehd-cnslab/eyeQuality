% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/batchConfig.R
\name{read_batch_config}
\alias{read_batch_config}
\title{Read a \code{batch_config.yaml} file}
\usage{
read_batch_config(path, validate = TRUE)
}
\arguments{
\item{path}{character(1) file path to read.}

\item{validate}{logical(1). When \code{TRUE} (the default), the merged config
is passed to \code{validate_batch_config()} before being returned, so a
structurally broken config is caught here rather than surfacing as a
confusing failure inside \code{listBidsFiles()}/\code{eyeQualityBatch()} later.
Set to \code{FALSE} to load a possibly-incomplete config as-is (e.g. to
repopulate a GUI form and let the user finish filling it in, then
validate separately once they're done).}
}
\value{
a named list; see the field list below. Every field
\code{default_batch_config()} defines is present (either from the file or
from that default).

\describe{
\item{\code{schemaVersion}}{integer, config schema version.}
\item{\code{batchName}}{character, passed to \code{eyeQualityBatch(batchName =)}.}
\item{\code{directoryBIDS}}{character, passed to
\code{eyeQualityBatch(directoryBIDS =)} / \code{listBidsFiles(directory =)}.}
\item{\code{layout}}{\code{"bids"} or \code{"glob"}, passed to
\code{listBidsFiles(layout =)}.}
\item{\code{subjectPattern_regex}, \code{sessionPattern_regex},
\code{recursiveSearch}}{\code{listBidsFiles()} \code{layout = "bids"} options.}
\item{\code{pathPattern}, \code{excludePattern_regex}}{\code{listBidsFiles()}
\code{layout = "glob"} options.}
\item{\code{modalityPattern_regex}}{\code{listBidsFiles()} option, used in
either layout mode.}
\item{\code{adapterType}}{character or \code{NULL}; the expected
\code{registered_adapters()} name (e.g. \code{"TobiiStudio"}) for this
batch's data. Informational only -- \code{eyeQuality()} detects the
adapter per file from file content (\code{detectImportSourceType()})
and this is not passed through as an override; recorded here for
reproducibility/documentation and checked against
\code{registered_adapters()} when set.}
\item{\code{displayDimensionX_mm}, \code{displayDimensionY_mm}}{numeric,
passed to \code{eyeQualityBatch()}/\code{eyeQuality()}.}
\item{\code{eyeSelection_method}}{one of \code{"Maximize"}, \code{"Strict"},
\code{"Left"}, \code{"Right"}, passed to \code{eyeQuality(eyeSelection_method =)}.}
\item{\code{validityThreshold}}{numeric on the 0-1 confidence scale, or
\code{NULL}, passed to \code{eyeQuality(validityThreshold =)}.}
\item{\code{outputDir}}{character or \code{NULL}, passed to
\code{eyeQualityBatch(outputDir =)}.}
\item{\code{outputStructure}}{\code{"flat"} (default) or \code{"bids"} (P7-07),
passed to \code{eyeQualityBatch(outputStructure =)}. Only meaningful
when \code{outputDir} is also set -- see \code{?create_new_filename} for the
full \code{"bids"}-mode layout. Absent entirely in any config written
before this field existed -- \code{"flat"} (this function's own
\code{default_batch_config()} fill-in) is the correct, fully
backward-compatible reading for that case, reproducing that
config's original output layout exactly.}
\item{\code{copyRawFile}}{logical (P7-07), passed to
\code{eyeQualityBatch(copyRawFile =)}. Only meaningful when
\code{outputStructure} is \code{"bids"} and \code{outputDir} is set. Absent
entirely in any config written before this field existed --
\code{FALSE} (this function's own \code{default_batch_config()} fill-in) is
the correct, fully backward-compatible reading for that case.}
\item{\code{numberCores}}{positive integer or \code{NULL}, passed to
\code{eyeQualityBatch(numberCores =)}.}
\item{\code{qcThresholds}}{named list or \code{NULL} (P10-07). Optional QC
threshold overrides from the Analyze tabs' threshold controls
(\code{inst/shiny-apps/app/analyze_helpers.R}'s \code{qc_threshold_config}),
keyed by \code{threshold_id} with each value a 0-100 percentage. Lets one
\code{batch_config.yaml} cover both a study's run parameters (Setup tab)
and its QC flagging settings (Analyze tabs) instead of splitting
into two files (both live in one app, \code{eyeQuality::runSetupApp()}/
\code{eyeQuality::runAnalyzeApp()}, since P10-12). Absent entirely in any
config written before this field existed -- \code{NULL} is the correct,
fully backward-compatible reading for that case.}
}
}
\description{
Reads \code{path} as YAML, fills in any field the file omits with
\code{default_batch_config()}'s defaults (so a config saved before a new
optional field was added still loads with sensible behavior for that
field), and by default validates the result before returning it.
}
