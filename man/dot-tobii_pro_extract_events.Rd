% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tobii-pro-adapter.R
\name{.tobii_pro_extract_events}
\alias{.tobii_pro_extract_events}
\title{Split standardized Tobii Pro data into gaze-stream and event rows}
\usage{
.tobii_pro_extract_events(data)
}
\arguments{
\item{data}{standardized data.frame (post \code{standardize()}).}
}
\value{
\code{list(gaze, events)} -- see \code{?eyeQuality-schema}.
}
\description{
Ported unchanged from the pre-Phase-3 \code{extractEventRows()}'s \code{TobiiPro}
branch (\code{R/extractEventRows.R}): gaze-stream rows are those where
\code{Sensor == "Eye Tracker"}; everything else (including \code{NA} \code{Sensor}) is
an event row.

P1-03 added a final \verb{else stop(...)} to the old \code{extractEventRows()} so
an unrecognized \code{software} value would fail loudly and immediately,
instead of leaving \code{gazeStreamData}/\code{eventData} unassigned and producing
an opaque "object not found" error downstream. That fix doesn't carry
forward as an explicit branch here: this method has no \code{software}
argument to dispatch on in the first place, because the adapter registry
(\code{detectImportSourceType()}, P3-03) is what selects \emph{which} adapter runs
at all -- calling \code{tobii_pro_adapter$extract_events()} IS the
"software == TobiiPro" case, by construction, so there is no fallthrough
branch left to guard. The underlying intent (fail clearly and
immediately rather than silently proceeding with bad/missing data) is
preserved structurally: if this method is ever called on data lacking a
\code{Sensor} column (e.g. mismatched/malformed input), \code{dplyr}'s
\code{.data$Sensor} lookup errors immediately, naming the missing column,
rather than silently returning wrong data.
}
\keyword{internal}
