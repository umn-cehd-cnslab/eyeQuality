% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tobii-studio-adapter.R
\name{.tobii_studio_extract_events}
\alias{.tobii_studio_extract_events}
\title{Split standardized Tobii Studio data into gaze-stream and event rows}
\usage{
.tobii_studio_extract_events(data)
}
\arguments{
\item{data}{standardized data.frame (post \code{standardize()}).}
}
\value{
\code{list(gaze, events)} -- see \code{?eyeQuality-schema}.
}
\description{
Ported unchanged from the pre-Phase-3 \code{extractEventRows()}'s
\code{TobiiStudio} branch (\code{R/extractEventRows.R}): gaze-stream rows are those
with a real \code{eyeTrackerTimestamp} (event/message rows carry the \code{-9999}
sentinel, or \code{NA}).

P1-03 added a final \verb{else stop(...)} to the old \code{extractEventRows()} so
an unrecognized \code{software} value would fail loudly and immediately,
instead of leaving \code{gazeStreamData}/\code{eventData} unassigned and producing
an opaque "object not found" error downstream. That fix doesn't carry
forward as an explicit branch here: this method has no \code{software}
argument to dispatch on in the first place, because the adapter registry
(\code{detectImportSourceType()}, P3-03) is what selects \emph{which} adapter runs
at all -- calling \code{tobii_studio_adapter$extract_events()} IS the
"software == TobiiStudio" case, by construction, so there is no
fallthrough branch left to guard. The underlying intent (fail clearly and
immediately rather than silently proceeding with bad/missing data) is
preserved structurally: if this method is ever called on data lacking an
\code{eyeTrackerTimestamp} column (e.g. mismatched/malformed input), \code{dplyr}'s
\code{.data$eyeTrackerTimestamp} lookup errors immediately, naming the missing
column, rather than silently returning wrong data.
}
\keyword{internal}
