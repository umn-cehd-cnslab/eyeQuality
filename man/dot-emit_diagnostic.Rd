% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/adapter-interface.R
\name{.emit_diagnostic}
\alias{.emit_diagnostic}
\title{Emit an opt-in verbose data-quality diagnostic message (P3-10)}
\usage{
.emit_diagnostic(text, verbose = FALSE)
}
\arguments{
\item{text}{character(1). The diagnostic message to emit (without a
leading "verbose:"-style tag -- this function adds a consistent prefix).}

\item{verbose}{logical(1). When not \code{TRUE}, this function is a no-op.}
}
\value{
\code{invisible(NULL)}.
}
\description{
Common helper called by adapter methods (\code{standardize()},
\code{extract_events()}, \code{normalize_validity()}) and by device-agnostic
pipeline stages (\code{classifyBlinks()}, \code{eyeSelection()},
\code{removeOffscreenGaze()}, \code{interpolateGaze()}) to report row/column-level,
data-quality-relevant events an end user wouldn't necessarily think to
check for themselves. A no-op unless \code{verbose} is \code{TRUE}, so call sites
don't need to guard every call with their own \verb{if (verbose)} check.

Uses \code{print()} (not \code{message()}), matching the same reporting convention
as \code{eyeQuality()}'s existing per-stage progress lines, so that when
\code{eyeQuality(saveData = TRUE)} sinks console output to a run log
(\code{sinkToOutputFile()}), diagnostics land in that same log rather than a
separate, unsunk stream. This is an entirely separate, non-fatal, opt-in
channel from P1-14's fix (which made existing hard-abort \code{stop()} calls
always carry a message); nothing here changes error/abort behavior.
}
\keyword{internal}
