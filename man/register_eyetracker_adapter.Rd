% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/adapter-interface.R
\name{register_eyetracker_adapter}
\alias{register_eyetracker_adapter}
\title{Register an eye tracker adapter}
\usage{
register_eyetracker_adapter(adapter)
}
\arguments{
\item{adapter}{an eye tracker adapter, typically produced by
\code{new_eyetracker_adapter()}. Validated via \code{validate_adapter()} before
being stored.}
}
\value{
\code{adapter}, invisibly.
}
\description{
Adds \code{adapter} to the package's internal adapter registry, keyed by
\code{adapter$name}. Registering an adapter under a name that is already
registered overwrites the previous entry (with a warning) -- this is
deliberate, so re-sourcing an adapter file during development or
re-registering a mock adapter in a test doesn't require restarting the
session.
}
