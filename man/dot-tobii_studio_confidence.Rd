% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tobii-studio-adapter.R
\name{.tobii_studio_confidence}
\alias{.tobii_studio_confidence}
\title{Compute the generic \code{confidence} (0-1) columns from Tobii Studio's native
\code{0}-\code{4} validity codes}
\usage{
.tobii_studio_confidence(data)
}
\arguments{
\item{data}{standardized data.frame with \code{validityLeft}/\code{validityRight}.}
}
\value{
\code{data} with new \code{confidenceLeft}/\code{confidenceRight} columns.
}
\description{
\code{confidenceLeft}/\code{confidenceRight <- 1 - (validityLeft/validityRight / 4)},
so \code{0} (best) maps to \code{1.0} and \code{4} (worst) maps to \code{0.0} -- see the
\code{confidence} entry in \code{?eyeQuality-schema} for the full 0-1 design
rationale. The schema documents \code{confidence} as a single conceptual
field but derives it separately per eye from \code{validityLeft}/
\code{validityRight}; this implementation follows every other per-eye pair in
the schema (\code{gazeLeftX}/\code{gazeRightX}, \code{pupilLeft}/\code{pupilRight}, etc.) and
emits \code{confidenceLeft}/\code{confidenceRight} rather than inventing an
unspecified left/right combination rule.

\strong{NA-validity-to-confidence mapping (P3-06 decision):} \code{confidence} is
set to \code{1} for \code{NA} validity, matching option (a) in the OPEN DECISION
section of \code{?eyeQuality-schema}. Rationale: the \emph{existing} \code{.valid}-
masking behavior (\code{.tobii_studio_mask_eye()} above, ported unchanged from
\code{removeInvalidGaze()}) already treats \code{NA} validity as unmasked --
effectively "valid" -- because \code{NA > threshold} is \code{NA}, and R's
\code{x[NA] <- value} subset-assignment is a no-op at \code{NA}-indexed positions.
\code{confidence = 1} for \code{NA} validity keeps this new column in agreement
with what masking actually does today, rather than introducing a second,
disagreeing notion of validity for the same input. This is a deliberate
choice, not a default: \code{confidence = 0} (treating unknown validity as
low-confidence) was the documented alternative, but it would not be zero
behavior change relative to the pipeline's current \emph{effective} semantics
for \code{NA}-validity samples, only zero behavior change relative to the
(arguably buggy) masking mechanism -- and since nothing downstream reads
\code{confidence} yet, internal consistency with \code{.valid}-masking was judged
the more conservative choice for a Phase 3 refactor whose explicit goal
is zero behavior change.
}
\keyword{internal}
