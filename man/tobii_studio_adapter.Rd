% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tobii-studio-adapter.R
\name{tobii_studio_adapter}
\alias{tobii_studio_adapter}
\title{Tobii Studio eye tracker adapter}
\description{
\code{detect()} is ported unchanged from the pre-Phase-3 \code{detectImportSourceType()}
\code{if}/\verb{else} branch: raw (pre-standardization) Tobii Studio exports carry a
\code{"StudioVersionRec"} column that no other supported device produces.

\code{standardize()} is ported unchanged (P3-04) from the pre-Phase-3
\code{renameColumns()}'s \code{TobiiStudio} branch (\code{R/renameColumns.R}): renames
the device-native columns onto the generic schema (\code{?eyeQuality-schema})
and splits \code{RecordingResolution} into \code{resolutionWidth}/\code{resolutionHeight}.

\code{extract_events()} is ported unchanged (P3-05) from the pre-Phase-3
\code{extractEventRows()}'s \code{TobiiStudio} branch (\code{R/extractEventRows.R}):
splits standardized data into gaze-stream vs. event rows using the
\code{eyeTrackerTimestamp == -9999} sentinel. Unlike the old function, this
adapter method takes no \code{software} argument to dispatch on -- the
registry (\code{detectImportSourceType()}, P3-03) already selects this adapter
specifically for Tobii Studio data, so there is no "unrecognized software
value" branch to carry forward here; see the roxygen note on
\code{.tobii_studio_extract_events()} below for the full reasoning.

\code{normalize_validity()} is ported unchanged (P3-06) from the pre-Phase-3
\code{removeInvalidGaze()}'s \code{TobiiStudio} branch (\code{R/removeInvalidGaze.R}):
for each eye, samples above \code{threshold} on Tobii Studio's native \code{0}-\code{4}
validity scale (\code{0} = best) are masked to \code{NA} in new \code{.valid}-suffixed
columns (P1-05's decision; raw columns are left untouched), plus the
\code{-9999} missing-value sentinel is masked the same way. It additionally
computes the new \code{confidenceLeft}/\code{confidenceRight} (0-1) columns from
\code{?eyeQuality-schema} -- see \code{.tobii_studio_confidence()} below for the
NA-validity mapping decision this task resolves.
}
\keyword{internal}
