% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/batchConfig.R
\name{validate_batch_config}
\alias{validate_batch_config}
\title{Validate a \code{batch_config.yaml}-shaped list}
\usage{
validate_batch_config(config)
}
\arguments{
\item{config}{a named list, typically from \code{read_batch_config()},
\code{default_batch_config()}, or built by hand/by a GUI form.}
}
\value{
\code{TRUE} (invisibly) if \code{config} is valid; otherwise throws an
error listing every problem found.
}
\description{
Checks that \code{config} has the structure and types
\code{read_batch_config()}/\code{write_batch_config()}/\code{eyeQualityBatch()} expect,
collecting every problem found (rather than stopping at the first) so a
broken or hand-edited config produces one clear, complete error instead
of a cryptic failure several call frames later -- the same approach
\code{validate_adapter()} (\code{R/adapter-interface.R}) takes for adapters.

Deliberately does not check that \code{directoryBIDS}/\code{outputDir} exist on
disk: a config is meant to be portable (e.g. authored on one machine,
run on another, or saved before the target directory is created), so
filesystem existence is left to \code{listBidsFiles()}/\code{eyeQualityBatch()} to
report when the config is actually used.
}
