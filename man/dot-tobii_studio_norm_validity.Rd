% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tobii-studio-adapter.R
\name{.tobii_studio_norm_validity}
\alias{.tobii_studio_norm_validity}
\title{Normalize Tobii Studio validity into \code{.valid}-masked columns and
\code{confidence}}
\usage{
.tobii_studio_norm_validity(data, threshold = NULL, verbose = FALSE)
}
\arguments{
\item{data}{standardized data.frame (post \code{standardize()}).}

\item{threshold}{numeric or \code{NULL}. On Tobii Studio's native \code{0}-\code{4}
validity scale. When \code{NULL}, uses this adapter's
\code{default_thresholds$validityThreshold} (\code{2}), matching the pre-Phase-3
\code{removeInvalidGaze()} default.}

\item{verbose}{logical(1). When \code{TRUE}, forwards to
\code{.tobii_studio_mask_eye()} for both eyes to emit P3-10 diagnostics.
Default \code{FALSE}.}
}
\value{
\code{data} with new \code{.valid}-suffixed masked columns and new
\code{confidenceLeft}/\code{confidenceRight} columns.
}
\description{
Ported unchanged from the pre-Phase-3 \code{removeInvalidGaze()}'s
\code{TobiiStudio} branch (\code{R/removeInvalidGaze.R}), operating on both eyes in
a single call per the adapter interface (\code{?new_eyetracker_adapter}).

The old \code{removeInvalidGaze()} took a \code{software} argument and had a final
\verb{else stop(...)} (P1-04) guarding against an unrecognized value, so bad
input failed loudly instead of silently returning unmodified data. That
explicit branch doesn't carry forward here for the same structural reason
documented on \code{.tobii_studio_extract_events()}: this method has no
\code{software} argument to dispatch on, because the adapter registry already
selected this adapter specifically for Tobii Studio data. The underlying
intent is preserved structurally instead: if this method is called on
data lacking \code{validityLeft}/\code{validityRight} columns (e.g. mismatched
input), \code{validityCol} resolves to \code{character(0)} inside
\code{.tobii_studio_mask_eye()} and the subsequent \code{data[[validityCol]]}
lookup errors immediately (\code{"subscript out of bounds"}), rather than
silently proceeding with no masking applied.
}
\keyword{internal}
