% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tobii-pro-adapter.R
\name{.tobii_pro_normalize_validity}
\alias{.tobii_pro_normalize_validity}
\title{Normalize Tobii Pro validity into \code{.valid}-masked columns and \code{confidence}}
\usage{
.tobii_pro_normalize_validity(data, threshold = NULL)
}
\arguments{
\item{data}{standardized data.frame (post \code{standardize()}).}

\item{threshold}{accepted but unused (see description).}
}
\value{
\code{data} with new \code{.valid}-suffixed masked columns and new
\code{confidenceLeft}/\code{confidenceRight} columns.
}
\description{
Ported unchanged from the pre-Phase-3 \code{removeInvalidGaze()}'s \code{TobiiPro}
branch (\code{R/removeInvalidGaze.R}), operating on both eyes in a single call
per the adapter interface (\code{?new_eyetracker_adapter}). \code{threshold} is
accepted for interface-signature compatibility but unused, matching the
pre-Phase-3 function: Tobii Pro's validity is a binary
\code{"Valid"}/\code{"Invalid"} string with no numeric threshold concept (see this
adapter's \code{default_thresholds}, an empty list).

The old \code{removeInvalidGaze()} took a \code{software} argument and had a final
\verb{else stop(...)} (P1-04) guarding against an unrecognized value, so bad
input failed loudly instead of silently returning unmodified data. That
explicit branch doesn't carry forward here for the same structural reason
documented on \code{.tobii_pro_extract_events()}: this method has no
\code{software} argument to dispatch on, because the adapter registry already
selected this adapter specifically for Tobii Pro data. The underlying
intent is preserved structurally instead: if this method is called on
data lacking \code{validityLeft}/\code{validityRight} columns (e.g. mismatched
input), \code{validityCol} resolves to \code{character(0)} inside
\code{.tobii_pro_mask_eye()} and the subsequent \code{data[, validityCol]} lookup
errors immediately (\code{"undefined columns selected"}), rather than silently
proceeding with no masking applied.
}
\keyword{internal}
