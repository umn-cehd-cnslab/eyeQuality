% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/adapter-interface.R
\name{validate_adapter}
\alias{validate_adapter}
\title{Validate that an object satisfies the eye tracker adapter contract}
\usage{
validate_adapter(adapter)
}
\arguments{
\item{adapter}{object to validate, typically produced by
\code{new_eyetracker_adapter()}.}
}
\value{
\code{TRUE} (invisibly) if \code{adapter} is valid; otherwise throws an
error listing every problem found.
}
\description{
Checks that \code{adapter} has all required fields, that the four interface
functions are actually functions accepting at least a \code{data} argument,
and that \code{geometry_type}/\code{default_thresholds} are well-formed. Collects
every problem found (rather than stopping at the first) so a broken
adapter definition produces one clear, complete error instead of a
cryptic failure several call frames later.
}
